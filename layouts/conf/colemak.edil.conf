# Remap 'Ctrl+E' to 'Ctrl+F'.
unmap buf 445
remap buf 446 mode_mov

# Remap 'Ctrl+Esc+K' to 'Ctrl+Esc+E'.
unmap buf 64b
remap buf 645 cmd_quit

# !!! The following is an invalid keybinding to 'Ctrl+I'.
#     Please remap this manually. The original keybinding
#     was 'Ctrl+L'
# unmap buf 44c
# unmap buf 449 mode_lng

# Remap 'Ctrl+N' to 'Ctrl+K'.
unmap buf 44e
remap buf 44b cmd_r_next

# !!! The following is an invalid keybinding to 'Ctrl+:'.
#     Please remap this manually. The original keybinding
#     was 'Ctrl+P'
# unmap buf 450
# unmap buf 43a cmd_r_paste

# Remap 'Esc+S' to 'Esc+R'.
unmap buf 253
remap buf 252 cmd_saveall

# Remap 'Ctrl+S' to 'Ctrl+R'.
unmap buf 453
remap buf 452 cmd_r_lineify

# !!! The following is an invalid keybinding to 'Ctrl+J'.
#     Please remap this manually. The original keybinding
#     was 'Ctrl+Y'
# unmap buf 459
# unmap buf 44a cmd_r_copy

# Remap 'Esc+g' to 'Esc+d'.
unmap buf 267
remap buf 264 cmd_goto

# Remap 'Esc+n' to 'Esc+k'.
unmap buf 26e
remap buf 26b cmd_new

# Remap 'Esc+s' to 'Esc+r'.
unmap buf 273
remap buf 272 cmd_r_save

# Remap 'Ctrl+E' to 'Ctrl+F'.
unmap bar 445
remap bar 446 mode_buf

# !!! The following is an invalid keybinding to 'Ctrl+I'.
#     Please remap this manually. The original keybinding
#     was 'Ctrl+L'
# unmap bar 44c
# unmap bar 449 mode_lng

# Remap ':' to 'O'.
unmap mov 3a
remap mov 4f mode_cmd

# Remap 'Ctrl+E' to 'Ctrl+F'.
unmap mov 445
remap mov 446 mode_buf

# Remap 'Ctrl+Esc+K' to 'Ctrl+Esc+E'.
unmap mov 64b
remap mov 645 cmd_quit

# !!! The following is an invalid keybinding to 'Ctrl+I'.
#     Please remap this manually. The original keybinding
#     was 'Ctrl+L'
# unmap mov 44c
# unmap mov 449 mode_lng

# Remap 'Ctrl+N' to 'Ctrl+K'.
unmap mov 44e
remap mov 44b cmd_r_next

# !!! The following is an invalid keybinding to 'Ctrl+:'.
#     Please remap this manually. The original keybinding
#     was 'Ctrl+P'
# unmap mov 450
# unmap mov 43a cmd_r_paste

# Remap 'Esc+S' to 'Esc+R'.
unmap mov 253
remap mov 252 cmd_saveall

# Remap 'Ctrl+S' to 'Ctrl+R'.
unmap mov 453
remap mov 452 cmd_r_lineify

# !!! The following is an invalid keybinding to 'Ctrl+J'.
#     Please remap this manually. The original keybinding
#     was 'Ctrl+Y'
# unmap mov 459
# unmap mov 44a cmd_r_copy

# Remap 'd' to 's'.
unmap mov 64
remap mov 73 cur_skp_r

# Remap 'e' to 'f'.
unmap mov 65
remap mov 66 cur_faster

# Remap 'f' to 't'.
unmap mov 66
remap mov 74 cur_pgup

# Remap 'g' to 'd'.
unmap mov 67
remap mov 64 cur_pgdn

# Remap 'Esc+g' to 'Esc+d'.
unmap mov 267
remap mov 264 cmd_goto

# Remap 'i' to 'u'.
unmap mov 69
remap mov 75 cur_end

# Remap 'j' to 'n'.
unmap mov 6a
remap mov 6e cur_mv_d

# Remap 'k' to 'e'.
unmap mov 6b
remap mov 65 cur_mv_r

# Remap 'Esc+n' to 'Esc+k'.
unmap mov 26e
remap mov 26b cmd_new

# Remap 's' to 'r'.
unmap mov 73
remap mov 72 cur_skp_d

# Remap 'Esc+s' to 'Esc+r'.
unmap mov 273
remap mov 272 cmd_r_save

# Remap 'u' to 'l'.
unmap mov 75
remap mov 6c cur_mv_u

# Remap 'y' to 'j'.
unmap mov 79
remap mov 6a cur_home

# Remap 'Ctrl+E' to 'Ctrl+F'.
unmap lng 445
remap lng 446 mode_buf

# Remap 'Ctrl+Esc+K' to 'Ctrl+Esc+E'.
unmap lng 64b
remap lng 645 cmd_quit

# !!! The following is an invalid keybinding to 'Ctrl+I'.
#     Please remap this manually. The original keybinding
#     was 'Ctrl+L'
# unmap lng 44c
# unmap lng 449 mode_buf

# Remap 'Ctrl+N' to 'Ctrl+K'.
unmap lng 44e
remap lng 44b cmd_r_next

# !!! The following is an invalid keybinding to 'Ctrl+:'.
#     Please remap this manually. The original keybinding
#     was 'Ctrl+P'
# unmap lng 450
# unmap lng 43a cmd_r_paste

# Remap 'Esc+S' to 'Esc+R'.
unmap lng 253
remap lng 252 cmd_saveall

# Remap 'Ctrl+S' to 'Ctrl+R'.
unmap lng 453
remap lng 452 cmd_r_lineify

# !!! The following is an invalid keybinding to 'Ctrl+J'.
#     Please remap this manually. The original keybinding
#     was 'Ctrl+Y'
# unmap lng 459
# unmap lng 44a cmd_r_copy

# Remap 'Esc+g' to 'Esc+d'.
unmap lng 267
remap lng 264 cmd_goto

# Remap 'Esc+n' to 'Esc+k'.
unmap lng 26e
remap lng 26b cmd_new

# Remap 'Esc+s' to 'Esc+r'.
unmap lng 273
remap lng 272 cmd_r_save

