# Remap 'Ctrl+C' to 'Ctrl+G'.
unmap buf 443
remap buf 447 cmd_r_swap

# Remap 'Ctrl+E' to 'Ctrl+B'.
unmap buf 445
remap buf 442 mode_mov

# Remap 'Ctrl+Esc+K' to 'Ctrl+Esc+A'.
unmap buf 64b
remap buf 641 cmd_quit

# Remap 'Ctrl+L' to 'Ctrl+Z'.
unmap buf 44c
remap buf 45a mode_lng

# Remap 'Ctrl+P' to 'Ctrl+^'.
unmap buf 450
remap buf 45e cmd_r_paste

# Remap 'Ctrl+S' to 'Ctrl+K'.
unmap buf 453
remap buf 44b cmd_r_lineify

# Remap 'Ctrl+Y' to 'Ctrl+R'.
unmap buf 459
remap buf 452 cmd_r_copy

# !!! The following is an invalid keybinding to 'Ctrl+"'. 
#     Please remap this manually. The original keybinding
#     was 'Ctrl+Z'
# unmap buf 45a
# unmap buf 422 cmd_r_snap

# !!! The following is an invalid keybinding to 'Ctrl+M'. 
#     Please remap this manually. The original keybinding
#     was 'Ctrl+^'
# unmap buf 45e
# unmap buf 44d mode_kcd

# Remap 'Esc+a' to 'Esc+-'.
unmap buf 261
remap buf 22d cmd_load

# Remap 'Esc+g' to 'Esc+t'.
unmap buf 267
remap buf 274 cmd_goto

# Remap 'Esc+m' to 'Esc+i'.
unmap buf 26d
remap buf 269 mode_mov

# Remap 'Esc+s' to 'Esc+k'.
unmap buf 273
remap buf 26b cmd_r_save

# !!! The following is an invalid keybinding to 'Ctrl+M'. 
#     Please remap this manually. The original keybinding
#     was 'Ctrl+^'
# unmap kcd 45e
# unmap kcd 44d mode_buf

# Remap 'Ctrl+A' to 'Ctrl+_'.
unmap bar 441
remap bar 45f mode_buf

# Remap 'Ctrl+K' to 'Ctrl+A'.
unmap bar 44b
remap bar 441 mode_kcd

# Remap ':' to '*'.
unmap mov 3a
remap mov 2a mode_cmd

# Remap 'Ctrl+C' to 'Ctrl+G'.
unmap mov 443
remap mov 447 cmd_r_swap

# Remap 'Ctrl+E' to 'Ctrl+B'.
unmap mov 445
remap mov 442 mode_buf

# Remap 'Ctrl+Esc+K' to 'Ctrl+Esc+A'.
unmap mov 64b
remap mov 641 cmd_quit

# Remap 'Ctrl+L' to 'Ctrl+Z'.
unmap mov 44c
remap mov 45a mode_lng

# Remap 'Ctrl+P' to 'Ctrl+^'.
unmap mov 450
remap mov 45e cmd_r_paste

# Remap 'Ctrl+S' to 'Ctrl+K'.
unmap mov 453
remap mov 44b cmd_r_lineify

# Remap 'Ctrl+Y' to 'Ctrl+R'.
unmap mov 459
remap mov 452 cmd_r_copy

# !!! The following is an invalid keybinding to 'Ctrl+"'. 
#     Please remap this manually. The original keybinding
#     was 'Ctrl+Z'
# unmap mov 45a
# unmap mov 422 cmd_r_snap

# !!! The following is an invalid keybinding to 'Ctrl+M'. 
#     Please remap this manually. The original keybinding
#     was 'Ctrl+^'
# unmap mov 45e
# unmap mov 44d mode_kcd

# Remap 'a' to '-'.
unmap mov 61
remap mov 2d cur_skp_l

# Remap 'Esc+a' to 'Esc+-'.
unmap mov 261
remap mov 22d cmd_load

# Remap 'd' to 'c'.
unmap mov 64
remap mov 63 cur_skp_r

# Remap 'e' to 'b'.
unmap mov 65
remap mov 62 cur_faster

# Remap 'f' to 'd'.
unmap mov 66
remap mov 64 cur_pgup

# Remap 'g' to 't'.
unmap mov 67
remap mov 74 cur_pgdn

# Remap 'Esc+g' to 'Esc+t'.
unmap mov 267
remap mov 274 cmd_goto

# Remap 'i' to 'o'.
unmap mov 69
remap mov 6f cur_end

# Remap 'j' to 'e'.
unmap mov 6a
remap mov 65 cur_mv_d

# Remap 'k' to 'a'.
unmap mov 6b
remap mov 61 cur_mv_r

# Remap 'Esc+m' to 'Esc+i'.
unmap mov 26d
remap mov 269 mode_buf

# Remap 'q' to ';'.
unmap mov 71
remap mov 3b cur_slower

# Remap 's' to 'k'.
unmap mov 73
remap mov 6b cur_skp_d

# Remap 'Esc+s' to 'Esc+k'.
unmap mov 273
remap mov 26b cmd_r_save

# Remap 'u' to 's'.
unmap mov 75
remap mov 73 cur_mv_u

# Remap 'w' to 'q'.
unmap mov 77
remap mov 71 cur_skp_u

# Remap 'y' to 'r'.
unmap mov 79
remap mov 72 cur_home

# Remap 'Ctrl+C' to 'Ctrl+G'.
unmap lng 443
remap lng 447 cmd_r_swap

# Remap 'Ctrl+E' to 'Ctrl+B'.
unmap lng 445
remap lng 442 mode_buf

# Remap 'Ctrl+Esc+K' to 'Ctrl+Esc+A'.
unmap lng 64b
remap lng 641 cmd_quit

# Remap 'Ctrl+L' to 'Ctrl+Z'.
unmap lng 44c
remap lng 45a mode_buf

# Remap 'Ctrl+P' to 'Ctrl+^'.
unmap lng 450
remap lng 45e cmd_r_paste

# Remap 'Ctrl+S' to 'Ctrl+K'.
unmap lng 453
remap lng 44b cmd_r_lineify

# Remap 'Ctrl+Y' to 'Ctrl+R'.
unmap lng 459
remap lng 452 cmd_r_copy

# !!! The following is an invalid keybinding to 'Ctrl+"'. 
#     Please remap this manually. The original keybinding
#     was 'Ctrl+Z'
# unmap lng 45a
# unmap lng 422 cmd_r_snap

# !!! The following is an invalid keybinding to 'Ctrl+M'. 
#     Please remap this manually. The original keybinding
#     was 'Ctrl+^'
# unmap lng 45e
# unmap lng 44d mode_kcd

# Remap 'Esc+a' to 'Esc+-'.
unmap lng 261
remap lng 22d cmd_load

# Remap 'Esc+g' to 'Esc+t'.
unmap lng 267
remap lng 274 cmd_goto

# Remap 'Esc+m' to 'Esc+i'.
unmap lng 26d
remap lng 269 mode_mov

# Remap 'Esc+s' to 'Esc+k'.
unmap lng 273
remap lng 26b cmd_r_save

